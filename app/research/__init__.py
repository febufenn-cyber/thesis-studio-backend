"""Research-instrument corpus (docs/LLD.md 3.8).

Exposes a de-identified, opt-in, deny-by-default corpus of how academic writing
is made. No project enters the corpus without an active, unrevoked, version-
pinned consent, and everything is anonymized (PII stripped, ids pseudonymized via
the privacy pepper) before it leaves the boundary.
"""
