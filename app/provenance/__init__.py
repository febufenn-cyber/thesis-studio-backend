"""AI provenance and authorship integrity (docs/LLD.md 3.1).

Aggregates block-level authorship origins and accepted AI-proposal history into a
provenance rollup, and renders it into a policy-mapped AI Use Statement. Origins
are derived from real command history and block metadata — never inferred by a
detector (roadmap non-goal).
"""
